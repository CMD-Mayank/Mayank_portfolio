import React from 'react';

// For the brutalist/clean aesthetic, we remove the busy particle field.
// The CSS noise texture in index.html handles the background texture.
export const BackgroundScene: React.FC = () => {
  return null;
};